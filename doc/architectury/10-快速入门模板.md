# 快速入门模板

本文档提供完整的 Architectury 模组模板，适用于 Minecraft 1.21.1 版本。

> 💡 **使用方法**：直接复制这些代码到你的项目中，把 `yourmod`、`yourname` 等替换成你自己的名字即可。

## 项目结构

```
your-mod/
├── common/
│   └── src/main/java/com/yourname/yourmod/
│       ├── YourMod.java
│       ├── init/
│       │   ├── ModItems.java
│       │   ├── ModBlocks.java
│       │   ├── ModEntities.java
│       │   └── ModTabs.java
│       └── item/
│           └── ExampleItem.java
├── fabric/
│   └── src/main/java/com/yourname/yourmod/
│       ├── fabric/
│       │   └── YourModFabric.java
│       └── platform/
│           └── PlatformHelperImpl.java
├── forge/
│   └── src/main/java/com/yourname/yourmod/
│       ├── forge/
│       │   └── YourModForge.java
│       └── platform/
│           └── PlatformHelperImpl.java
├── neoforge/
│   └── src/main/java/com/yourname/yourmod/
│       ├── neoforge/
│       │   └── YourModNeoForge.java
│       └── platform/
│           └── PlatformHelperImpl.java
├── build.gradle
├── settings.gradle
└── gradle.properties
```

## 配置文件

### gradle.properties

```properties
# 版本配置
minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.22)
fabric_version=0.16.0
fabric_loader_version=0.16.9
forge_version=52.0.16
neoforge_version=21.1.77

# 模组信息
mod_version=1.0.0
maven_group=com.yourname
archives_base_name=yourmod

# Architectury
architectury_version=14.0.3
```

### settings.gradle

```groovy
pluginManagement {
    repositories {
        maven { url = 'https://maven.architectury.dev/' }
        maven { url = 'https://maven.fabricmc.net/' }
        maven { url = 'https://maven.neoforged.net/releases/' }
        gradlePluginPortal()
        mavenCentral()
    }
}

include('common', 'fabric', 'forge', 'neoforge')
```

### build.gradle (根目录)

```groovy
plugins {
    id "architectury-plugin" version "3.4-SNAPSHOT"
    id "dev.architectury.loom" version "1.7-SNAPSHOT" apply false
    id 'io.github.juuxel.loom-quiltflower' version '1.+' apply false
}

architectury {
    minecraft = rootProject.minecraft_version
}

subprojects {
    apply plugin: "java"
    apply plugin: "architectury-plugin"
    apply plugin: "maven-publish"
    
    version = rootProject.mod_version
    group = rootProject.maven_group
    
    repositories {
        maven { name = 'ParchmentMC'; url = 'https://maven.parchmentmc.org' }
    }
    
    dependencies {
        minecraft "com.mojang:minecraft:${rootProject.minecraft_version}"
        mappings loom.layered() {
            officialMojangMappings()
            parchment("org.parchmentmc.data:parchment-${rootProject.minecraft_version}:2024.11.17@zip")
        }
    }
}
```

## Common 模块

### common/build.gradle

```groovy
plugins {
    id "dev.architectury.loom"
}

loom {
    silentMojangMappingsLicense()
}

dependencies {
    minecraft "com.mojang:minecraft:${rootProject.minecraft_version}"
    mappings loom.layered() {
        officialMojangMappings()
        parchment("org.parchmentmc.data:parchment-${rootProject.minecraft_version}:2024.11.17@zip")
    }
    
    compileOnly "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"
    modImplementation "dev.architectury:architectury:${rootProject.architectury_version}"
}

architectury {
    common(rootProject.enabled_platforms.split(","))
}
```

### YourMod.java

```java
package com.yourname.yourmod;

import com.yourname.yourmod.init.ModBlocks;
import com.yourname.yourmod.init.ModEntities;
import com.yourname.yourmod.init.ModItems;
import com.yourname.yourmod.init.ModTabs;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class YourMod {
    public static final String MOD_ID = "yourmod";
    public static final Logger LOGGER = LoggerFactory.getLogger(MOD_ID);
    
    public static void init() {
        LOGGER.info("YourMod initializing...");
        
        ModTabs.register();
        ModItems.register();
        ModBlocks.register();
        ModEntities.register();
        
        LOGGER.info("YourMod initialized!");
    }
}
```

### ModItems.java

```java
package com.yourname.yourmod.init;

import dev.architectury.core.item.ArchitecturySpawnEggItem;
import dev.architectury.registry.registries.DeferredRegister;
import dev.architectury.registry.registries.RegistrySupplier;
import net.minecraft.core.registries.Registries;
import net.minecraft.world.item.Item;

import static com.yourname.yourmod.YourMod.MOD_ID;

public class ModItems {
    public static final DeferredRegister<Item> ITEMS = 
        DeferredRegister.create(MOD_ID, Registries.ITEM);
    
    public static final RegistrySupplier<Item> EXAMPLE_ITEM = 
        ITEMS.register("example_item", 
            () -> new Item(new Item.Properties().arch$tab(ModTabs.EXAMPLE_TAB)));
    
    public static final RegistrySupplier<Item> EXAMPLE_SPAWN_EGG = 
        ITEMS.register("example_spawn_egg", 
            () -> new ArchitecturySpawnEggItem(
                ModEntities.EXAMPLE_ENTITY, 
                0xFF0000, 
                0x00FF00, 
                new Item.Properties().arch$tab(ModTabs.EXAMPLE_TAB)));
    
    public static void register() {
        ITEMS.register();
    }
}
```

### ModBlocks.java

```java
package com.yourname.yourmod.init;

import dev.architectury.registry.registries.DeferredRegister;
import dev.architectury.registry.registries.RegistrySupplier;
import net.minecraft.core.registries.Registries;
import net.minecraft.world.item.BlockItem;
import net.minecraft.world.item.Item;
import net.minecraft.world.level.block.Block;
import net.minecraft.world.level.block.state.BlockBehaviour;

import static com.yourname.yourmod.YourMod.MOD_ID;

public class ModBlocks {
    public static final DeferredRegister<Block> BLOCKS = 
        DeferredRegister.create(MOD_ID, Registries.BLOCK);
    
    public static final DeferredRegister<Item> ITEMS = 
        DeferredRegister.create(MOD_ID, Registries.ITEM);
    
    public static final RegistrySupplier<Block> EXAMPLE_BLOCK = 
        BLOCKS.register("example_block", 
            () -> new Block(BlockBehaviour.Properties.of()
                .strength(3.0F, 3.0F)
                .requiresCorrectToolForDrops()));
    
    public static final RegistrySupplier<Item> EXAMPLE_BLOCK_ITEM = 
        ITEMS.register("example_block", 
            () -> new BlockItem(EXAMPLE_BLOCK.get(), 
                new Item.Properties().arch$tab(ModTabs.EXAMPLE_TAB)));
    
    public static void register() {
        BLOCKS.register();
        ITEMS.register();
    }
}
```

### ModEntities.java

```java
package com.yourname.yourmod.init;

import dev.architectury.registry.registries.DeferredRegister;
import dev.architectury.registry.registries.RegistrySupplier;
import net.minecraft.core.registries.Registries;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.world.entity.EntityType;
import net.minecraft.world.entity.MobCategory;

import static com.yourname.yourmod.YourMod.MOD_ID;

public class ModEntities {
    public static final DeferredRegister<EntityType<?>> ENTITIES = 
        DeferredRegister.create(MOD_ID, Registries.ENTITY_TYPE);
    
    public static final RegistrySupplier<EntityType<ExampleEntity>> EXAMPLE_ENTITY = 
        ENTITIES.register("example_entity", 
            () -> EntityType.Builder.of(ExampleEntity::new, MobCategory.CREATURE)
                .sized(0.9F, 1.4F)
                .build(new ResourceLocation(MOD_ID, "example_entity").toString()));
    
    public static void register() {
        ENTITIES.register();
    }
}
```

### ModTabs.java

```java
package com.yourname.yourmod.init;

import dev.architectury.registry.registries.DeferredRegister;
import dev.architectury.registry.registries.RegistrySupplier;
import dev.architectury.registry.CreativeTabRegistry;
import net.minecraft.core.registries.Registries;
import net.minecraft.network.chat.Component;
import net.minecraft.world.item.CreativeModeTab;
import net.minecraft.world.item.ItemStack;

import static com.yourname.yourmod.YourMod.MOD_ID;

public class ModTabs {
    public static final DeferredRegister<CreativeModeTab> TABS = 
        DeferredRegister.create(MOD_ID, Registries.CREATIVE_MODE_TAB);
    
    public static final RegistrySupplier<CreativeModeTab> EXAMPLE_TAB = 
        TABS.register("example_tab", 
            () -> CreativeTabRegistry.create(
                Component.translatable("itemGroup.yourmod.example_tab"),
                () -> new ItemStack(ModItems.EXAMPLE_ITEM.get())));
    
    public static void register() {
        TABS.register();
    }
}
```

### ExampleItem.java

```java
package com.yourname.yourmod.item;

import net.minecraft.world.InteractionHand;
import net.minecraft.world.InteractionResultHolder;
import net.minecraft.world.entity.player.Player;
import net.minecraft.world.item.Item;
import net.minecraft.world.item.ItemStack;
import net.minecraft.world.level.Level;

public class ExampleItem extends Item {
    public ExampleItem(Properties properties) {
        super(properties);
    }
    
    @Override
    public InteractionResultHolder<ItemStack> use(Level level, Player player, InteractionHand hand) {
        ItemStack stack = player.getItemInHand(hand);
        
        if (!level.isClientSide) {
            // 服务器端逻辑
            player.sendMessage(Component.literal("You used the example item!"), 
                player.getUUID());
        }
        
        return InteractionResultHolder.sidedSuccess(stack, level.isClientSide);
    }
}
```

### ExampleEntity.java

```java
package com.yourname.yourmod.entity;

import net.minecraft.world.entity.EntityType;
import net.minecraft.world.entity.Mob;
import net.minecraft.world.entity.ai.attributes.AttributeSupplier;
import net.minecraft.world.entity.ai.attributes.Attributes;
import net.minecraft.world.level.Level;

public class ExampleEntity extends Mob {
    public ExampleEntity(EntityType<? extends Mob> entityType, Level level) {
        super(entityType, level);
    }
    
    public static AttributeSupplier.Builder createAttributes() {
        return Mob.createMobAttributes()
            .add(Attributes.MAX_HEALTH, 20.0)
            .add(Attributes.MOVEMENT_SPEED, 0.25)
            .add(Attributes.FOLLOW_RANGE, 32.0);
    }
}
```

### PlatformHelper.java

```java
package com.yourname.yourmod.platform;

import dev.architectury.injectables.annotations.ExpectPlatform;

import java.io.File;

public class PlatformHelper {
    @ExpectPlatform
    public static File getConfigDirectory() {
        throw new AssertionError();
    }
    
    @ExpectPlatform
    public static boolean isModLoaded(String modId) {
        throw new AssertionError();
    }
}
```

## Fabric 模块

### fabric/build.gradle

```groovy
plugins {
    id "dev.architectury.loom"
}

dependencies {
    minecraft "com.mojang:minecraft:${rootProject.minecraft_version}"
    mappings loom.layered() {
        officialMojangMappings()
        parchment("org.parchmentmc.data:parchment-${rootProject.minecraft_version}:2024.11.17@zip")
    }
    
    modImplementation "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"
    modImplementation "net.fabricmc.fabric-api:fabric-api:${rootProject.fabric_version}"
    modImplementation "dev.architectury:architectury:${rootProject.architectury_version}"
    
    implementation project(path: ':common', configuration: 'namedElements')
    runtimeOnly(project(path: ':common', configuration: 'transformProductionFabric'))
    compileOnly(project(path: ':common', configuration: 'transformProductionFabric'))
}

architectury {
    platformSetupLoomIde()
    fabric()
}
```

### YourModFabric.java

```java
package com.yourname.yourmod.fabric;

import com.yourname.yourmod.YourMod;
import com.yourname.yourmod.entity.ExampleEntity;
import com.yourname.yourmod.init.ModEntities;
import net.fabricmc.api.ModInitializer;
import net.fabricmc.fabric.api.object.builder.v1.entity.FabricDefaultAttributeRegistry;

public class YourModFabric implements ModInitializer {
    @Override
    public void onInitialize() {
        YourMod.init();
        
        // 注册实体属性
        FabricDefaultAttributeRegistry.register(
            ModEntities.EXAMPLE_ENTITY.get(), 
            ExampleEntity.createAttributes());
    }
}
```

### PlatformHelperImpl.java (Fabric)

```java
package com.yourname.yourmod.platform.fabric;

import net.fabricmc.loader.api.FabricLoader;

import java.io.File;

public class PlatformHelperImpl {
    public static File getConfigDirectory() {
        return FabricLoader.getInstance().getConfigDir().toFile();
    }
    
    public static boolean isModLoaded(String modId) {
        return FabricLoader.getInstance().isModLoaded(modId);
    }
}
```

### fabric.mod.json

```json
{
  "schemaVersion": 1,
  "id": "yourmod",
  "version": "${version}",
  "name": "Your Mod",
  "description": "Your mod description",
  "authors": ["Your Name"],
  "contact": {
    "homepage": "https://example.com",
    "sources": "https://github.com/yourname/yourmod"
  },
  "license": "MIT",
  "icon": "assets/yourmod/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": ["com.yourname.yourmod.fabric.YourModFabric"]
  },
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": "~1.21.1",
    "java": ">=17",
    "architectury": "*"
  }
}
```

## Forge 模块

### forge/build.gradle

```groovy
plugins {
    id "dev.architectury.loom"
}

dependencies {
    minecraft "com.mojang:minecraft:${rootProject.minecraft_version}"
    mappings loom.layered() {
        officialMojangMappings()
        parchment("org.parchmentmc.data:parchment-${rootProject.minecraft_version}:2024.11.17@zip")
    }
    
    forge "net.minecraftforge:forge:${rootProject.forge_version}"
    modImplementation "dev.architectury:architectury:${rootProject.architectury_version}"
    
    implementation project(path: ':common', configuration: 'namedElements')
    runtimeOnly(project(path: ':common', configuration: 'transformProductionForge'))
    compileOnly(project(path: ':common', configuration: 'transformProductionForge'))
}

architectury {
    platformSetupLoomIde()
    forge()
}
```

### YourModForge.java

```java
package com.yourname.yourmod.forge;

import com.yourname.yourmod.YourMod;
import com.yourname.yourmod.entity.ExampleEntity;
import com.yourname.yourmod.init.ModEntities;
import dev.architectury.platform.forge.EventBuses;
import net.minecraftforge.event.entity.EntityAttributeCreationEvent;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.fml.javafmlmod.FMLJavaModLoadingContext;

@Mod(YourMod.MOD_ID)
public class YourModForge {
    public YourModForge() {
        EventBuses.registerModEventBus(YourMod.MOD_ID, 
            FMLJavaModLoadingContext.get().getModEventBus());
        
        YourMod.init();
        
        FMLJavaModLoadingContext.get().getModEventBus().addListener(this::registerAttributes);
    }
    
    private void registerAttributes(EntityAttributeCreationEvent event) {
        event.put(ModEntities.EXAMPLE_ENTITY.get(), 
            ExampleEntity.createAttributes().build());
    }
}
```

### PlatformHelperImpl.java (Forge)

```java
package com.yourname.yourmod.platform.forge;

import net.minecraftforge.fml.ModList;
import net.minecraftforge.fml.loading.FMLPaths;

import java.io.File;

public class PlatformHelperImpl {
    public static File getConfigDirectory() {
        return FMLPaths.CONFIGDIR.get().toFile();
    }
    
    public static boolean isModLoaded(String modId) {
        return ModList.get().isLoaded(modId);
    }
}
```

### mods.toml

```toml
modLoader = "javafml"
loaderVersion = "[52,)"
license = "MIT"

[[mods]]
modId = "yourmod"
version = "${version}"
displayName = "Your Mod"
description = "Your mod description"
authors = "Your Name"

[[dependencies.yourmod]]
modId = "forge"
mandatory = true
versionRange = "[52,)"
ordering = "NONE"
side = "BOTH"

[[dependencies.yourmod]]
modId = "minecraft"
mandatory = true
versionRange = "[1.21.1,1.22)"
ordering = "NONE"
side = "BOTH"

[[dependencies.yourmod]]
modId = "architectury"
mandatory = true
versionRange = "[14.0,)"
ordering = "AFTER"
side = "BOTH"
```

## NeoForge 模块

### neoforge/build.gradle

```groovy
plugins {
    id "dev.architectury.loom"
}

dependencies {
    minecraft "com.mojang:minecraft:${rootProject.minecraft_version}"
    mappings loom.layered() {
        officialMojangMappings()
        parchment("org.parchmentmc.data:parchment-${rootProject.minecraft_version}:2024.11.17@zip")
    }
    
    neoForge "net.neoforged:neoforge:${rootProject.neoforge_version}"
    modImplementation "dev.architectury:architectury:${rootProject.architectury_version}"
    
    implementation project(path: ':common', configuration: 'namedElements')
    runtimeOnly(project(path: ':common', configuration: 'transformProductionNeoForge'))
    compileOnly(project(path: ':common', configuration: 'transformProductionNeoForge'))
}

architectury {
    platformSetupLoomIde()
    neoforge()
}
```

### YourModNeoForge.java

```java
package com.yourname.yourmod.neoforge;

import com.yourname.yourmod.YourMod;
import com.yourname.yourmod.entity.ExampleEntity;
import com.yourname.yourmod.init.ModEntities;
import dev.architectury.platform.neoforge.EventBuses;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.fml.common.Mod;
import net.neoforged.neoforge.event.entity.EntityAttributeCreationEvent;

@Mod(YourMod.MOD_ID)
public class YourModNeoForge {
    public YourModNeoForge(IEventBus eventBus) {
        EventBuses.registerModEventBus(YourMod.MOD_ID, eventBus);
        
        YourMod.init();
        
        eventBus.addListener(this::registerAttributes);
    }
    
    private void registerAttributes(EntityAttributeCreationEvent event) {
        event.put(ModEntities.EXAMPLE_ENTITY.get(), 
            ExampleEntity.createAttributes().build());
    }
}
```

### PlatformHelperImpl.java (NeoForge)

```java
package com.yourname.yourmod.platform.neoforge;

import net.neoforged.fml.ModList;
import net.neoforged.fml.loading.FMLPaths;

import java.io.File;

public class PlatformHelperImpl {
    public static File getConfigDirectory() {
        return FMLPaths.CONFIGDIR.get().toFile();
    }
    
    public static boolean isModLoaded(String modId) {
        return ModList.get().isLoaded(modId);
    }
}
```

### neoforge.mods.toml

```toml
modLoader = "javafml"
loaderVersion = "[4,)"
license = "MIT"

[[mods]]
modId = "yourmod"
version = "${version}"
displayName = "Your Mod"
description = "Your mod description"
authors = "Your Name"

[[dependencies.yourmod]]
modId = "neoforge"
mandatory = true
versionRange = "[21.1,)"
ordering = "NONE"
side = "BOTH"

[[dependencies.yourmod]]
modId = "minecraft"
mandatory = true
versionRange = "[1.21.1,1.22)"
ordering = "NONE"
side = "BOTH"

[[dependencies.yourmod]]
modId = "architectury"
mandatory = true
versionRange = "[14.0,)"
ordering = "AFTER"
side = "BOTH"
```

## 使用模板

### 1. 下载或克隆模板

```bash
git clone https://github.com/architectury/architectury-examplemod.git your-mod
cd your-mod
```

### 2. 修改配置

1. 更新 `gradle.properties` 中的模组信息
2. 更新包名（将 `com.yourname.yourmod` 替换为你的包名）
3. 更新 `mods.toml` / `fabric.mod.json` / `neoforge.mods.toml`

### 3. 生成 IDE 配置

```bash
# IntelliJ IDEA
./gradlew genIntellijRuns

# Eclipse
./gradlew genEclipseRuns

# VS Code
./gradlew vscode
```

### 4. 运行测试

```bash
# Fabric 客户端
./gradlew :fabric:runClient

# Forge 客户端
./gradlew :forge:runClient

# NeoForge 客户端
./gradlew :neoforge:runClient
```

### 5. 构建模组

```bash
./gradlew build
```

构建产物位于：
- `fabric/build/libs/yourmod-fabric-1.0.0.jar`
- `forge/build/libs/yourmod-forge-1.0.0.jar`
- `neoforge/build/libs/yourmod-neoforge-1.0.0.jar`

## 扩展模板

### 添加新物品

```java
// 在 ModItems.java 中添加
public static final RegistrySupplier<Item> NEW_ITEM = 
    ITEMS.register("new_item", 
        () -> new NewItem(new Item.Properties().arch$tab(ModTabs.EXAMPLE_TAB)));
```

### 添加新方块

```java
// 在 ModBlocks.java 中添加
public static final RegistrySupplier<Block> NEW_BLOCK = 
    BLOCKS.register("new_block", 
        () -> new NewBlock(BlockBehaviour.Properties.of()
            .strength(3.0F, 3.0F)));

public static final RegistrySupplier<Item> NEW_BLOCK_ITEM = 
    ITEMS.register("new_block", 
        () -> new BlockItem(NEW_BLOCK.get(), 
            new Item.Properties().arch$tab(ModTabs.EXAMPLE_TAB)));
```

### 添加事件监听

```java
// 在 YourMod.java 的 init() 方法中添加
EntityEvent.LIVING_HURT.register((entity, source, amount) -> {
    // 处理实体受伤事件
    return EventResult.pass();
});
```

### 添加网络消息

```java
public static final NetworkChannel CHANNEL = 
    NetworkChannel.create(new ResourceLocation(MOD_ID, "main"));

// 在 init() 中注册
CHANNEL.register(ExampleMessage.class, 
    ExampleMessage::encode, 
    ExampleMessage::new, 
    ExampleMessage::apply);
```

## 相关资源

- [Architectury 官方文档](https://docs.architectury.dev/)
- [Architectury 示例模组](https://github.com/architectury/architectury-examplemod)
- [Discord 社区](https://discord.architectury.dev/)
